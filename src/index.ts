import { Button } from "./components/Button/Button";
import { Input } from "./components/Input/Input";

// Export individual components
export { Button, Input };

// Export as a single object for UMD
export default {
  Button,
  Input,
};
